/**
 * Created by anilkatta on 3/19/16.
 */

'use strict';

// function getImageSource(text : Object, kind: string) {uri: string} {
// 	var uri = movie && movie.posters ? movie.posters.thumbnail : null;
// 	if (uri && kind) {
// 		uri = uri.replace('tmb', kind);
// 	}
// 	return { uri };
// };
//
// module.exports = getImageSource;